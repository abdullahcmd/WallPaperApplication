import React from "react";
import { View, Text, StyleSheet } from "react-native";
const ParaGraph = ({ content, style1 }) => {
  return <Text style={style1}>{content}</Text>;
};

export default ParaGraph;
